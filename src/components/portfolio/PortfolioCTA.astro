<section id="portfolio-section" class="portfolio-section">
  <p class="portfolio-sub">더 다양한 경험을 자세히 담았습니다.</p>
  <a href="/p" class="portfolio-btn">
    포트폴리오 보러가기
    <i class="bi bi-arrow-right"></i>
  </a>
  <a href="#contact" class="contact-btn">연락처 보기</a>
</section>

<style>
  .portfolio-section {
    margin-top: 4rem;
    padding: 2.5rem 0 6rem;
    text-align: center;
    border-top: 1px solid var(--border);
  }
  .portfolio-sub {
    font-size: 0.875rem;
    color: var(--text-muted);
    margin-bottom: 1.5rem;
  }
  .portfolio-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.65rem 1.5rem;
    border-radius: 8px;
    background: var(--accent);
    color: var(--bg);
    font-size: 0.875rem;
    font-weight: 700;
    text-decoration: none;
    transition:
      opacity 0.15s,
      transform 0.15s;
  }
  .portfolio-btn:hover {
    opacity: 0.85;
    transform: translateY(-1px);
  }
  .portfolio-btn i {
    font-size: 0.9rem;
  }
  .contact-btn {
    display: block;
    width: fit-content;
    margin: 0.75rem auto 0;
    color: var(--text-muted);
    font-size: 0.78rem;
    font-weight: 600;
    text-decoration: none;
    transition: color 0.15s;
  }
  .contact-btn:hover {
    color: var(--accent);
    text-decoration: underline;
    text-underline-offset: 3px;
  }
</style>
